import { Component } from '@angular/core';
import { Switch } from '../switch/switch';

@Component({
  selector: 'cFooter',
  imports: [Switch],
  templateUrl: './footer.html'
})
export class Footer {
  
}
